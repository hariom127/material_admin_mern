'use strict'
const bcrypt = require('bcrypt')

var exportFuns = {}

//============================================================================

// exportFuns.comparePassword = async (enteredPassword, password) => {
//   return await bcrypt.compare(enteredPassword, password)
// }

module.exports = exportFuns
